sources = Split("""boolean.c
					core.c
					integer.c
					object.c
					string.c""")

CFLAGS = Split("""-ansi
					-Wall""")

LIBS = Split("""gc
				""")



Library('kitsune', sources,
		LIBS = LIBS,
		CFLAGS = CFLAGS)
